name: Grab free ARM (A1.Flex)

# Polls Oracle Cloud every ~10 minutes and launches the free Always-Free
# ARM instance the moment Singapore has capacity. Stops launching once an
# instance named "storetrack" exists, and opens an issue to notify you.

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes (UTC)
  workflow_dispatch:
    inputs:
      test_notify:
        description: "Post a test notification issue (as the bot) and exit — verifies the alert reaches you"
        type: boolean
        default: false

permissions:
  contents: read
  issues: write

concurrency:
  group: oci-arm-hunt
  cancel-in-progress: false

jobs:
  hunt:
    runs-on: ubuntu-latest
    env:
      # OCI CLI reads these env vars directly (keyless auth, no config file).
      OCI_CLI_USER: ${{ secrets.OCI_CLI_USER }}
      OCI_CLI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
      OCI_CLI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
      OCI_CLI_KEY_CONTENT: ${{ secrets.OCI_CLI_KEY_CONTENT }}
      OCI_CLI_REGION: ${{ secrets.OCI_CLI_REGION }}
      SUPPRESS_LABEL_WARNING: "True"
      AD: "jpjp:AP-SINGAPORE-1-AD-1"
      SHAPE: "VM.Standard.A1.Flex"
      SHAPE_CONFIG: '{"ocpus": 2, "memoryInGBs": 12}'
      NAME: "storetrack"
    steps:
      - name: Notification self-test
        if: ${{ inputs.test_notify }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          num=$(gh issue create -R "${{ github.repository }}" \
            --title "🔔 Notification test (bot) — auto-closed" \
            --assignee Nhi21pham \
            --body "@Nhi21pham This issue was created by github-actions[bot], the exact actor and code path as the real '🎉 acquired' alert. If you received an email/notification for this, the real one will reach you too." \
            | grep -oE '[0-9]+$')
          gh issue close "$num" -R "${{ github.repository }}" --comment "Auto-closed: notification self-test complete."
          echo "Created and closed test issue #$num as the bot."

      - name: Install OCI CLI
        if: ${{ !inputs.test_notify }}
        run: pip install --quiet oci-cli

      - name: Attempt launch
        id: launch
        if: ${{ !inputs.test_notify }}
        run: |
          set -uo pipefail
          TENANCY="$OCI_CLI_TENANCY"
          SUBNET="${{ secrets.OCI_SUBNET_ID }}"
          IMAGE="${{ secrets.OCI_IMAGE_ID }}"
          printf '%s\n' "${{ secrets.OCI_SSH_PUBKEY }}" > /tmp/id.pub

          # Guard: if a non-terminated "storetrack" instance already exists, do nothing.
          existing=$(oci --no-retry compute instance list --compartment-id "$TENANCY" \
            --query "data[?\"display-name\"=='$NAME' && \"lifecycle-state\"!='TERMINATED'].id | [0]" \
            --raw-output 2>/dev/null || true)
          if [ -n "${existing:-}" ] && [ "$existing" != "null" ]; then
            echo "Instance '$NAME' already exists ($existing). Nothing to do."
            exit 0
          fi

          out=$(oci --no-retry compute instance launch \
            --availability-domain "$AD" --compartment-id "$TENANCY" \
            --shape "$SHAPE" --shape-config "$SHAPE_CONFIG" \
            --image-id "$IMAGE" --subnet-id "$SUBNET" --assign-public-ip true \
            --display-name "$NAME" --ssh-authorized-keys-file /tmp/id.pub 2>&1 || true)
          echo "$out"

          if echo "$out" | grep -q '"id": "ocid1.instance'; then
            iid=$(echo "$out" | grep -m1 '"id": "ocid1.instance' | sed 's/.*: "\(.*\)".*/\1/')
            echo "got_it=true" >> "$GITHUB_OUTPUT"
            echo "iid=$iid" >> "$GITHUB_OUTPUT"
          elif echo "$out" | grep -qiE "out of host capacity|too ?many ?requests|\"status\": 429"; then
            echo "::notice::No ARM capacity yet (expected). Will retry next run."
          else
            echo "::error::Unexpected launch error (see log above)."
            exit 1
          fi

      - name: Resolve public IP
        id: ip
        if: steps.launch.outputs.got_it == 'true'
        run: |
          set -uo pipefail
          iid="${{ steps.launch.outputs.iid }}"
          ip=""
          # VNIC + public IP attach a little after launch; poll up to ~2 min.
          for i in $(seq 1 24); do
            ip=$(oci --no-retry compute instance list-vnics \
              --instance-id "$iid" --compartment-id "$OCI_CLI_TENANCY" \
              --query 'data[0]."public-ip"' --raw-output 2>/dev/null || true)
            if [ -n "$ip" ] && [ "$ip" != "null" ]; then break; fi
            sleep 5
          done
          echo "ip=$ip" >> "$GITHUB_OUTPUT"
          echo "Public IP: ${ip:-<not ready — check OCI console>}"

      - name: Announce success
        if: steps.launch.outputs.got_it == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          iid="${{ steps.launch.outputs.iid }}"
          ip="${{ steps.ip.outputs.ip }}"
          [ -n "$ip" ] && [ "$ip" != "null" ] || ip="(not ready yet — see OCI console)"
          printf '%s\n' \
            "@Nhi21pham A free VM.Standard.A1.Flex (2 OCPU / 12 GB) just launched 🎉" \
            "" \
            "Public IP:     $ip" \
            "SSH:           ssh ubuntu@$ip" \
            "Instance OCID: $iid" \
            "" \
            "Next — migrate StoreTrack onto it. Step-by-step runbook:" \
            "https://github.com/${{ github.repository }}/blob/main/MIGRATION.md" \
            "" \
            "This workflow now idles (the guard skips relaunching while the instance exists)." \
            > /tmp/body.md
          gh issue create \
            --title "🎉 Free ARM instance acquired" \
            --assignee Nhi21pham \
            --body-file /tmp/body.md \
            || echo "issue create failed (non-fatal)"
